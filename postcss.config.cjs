// Standard PostCSS config for Tailwind: use the `tailwindcss` plugin which
// is provided by the `tailwindcss` package itself.
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
}
